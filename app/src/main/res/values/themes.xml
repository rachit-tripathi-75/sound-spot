<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.soundspottheme" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:statusBarColor">@color/background</item>
        <item name="android:navigationBarColor">@color/background</item>
        <item name="android:windowLightNavigationBar">true</item>

        <!-- Light theme colors -->
        <item name="colorPrimary">@color/spotify_green</item>
        <item name="colorPrimaryVariant">@color/spotify_light_green</item>
        <item name="colorOnPrimary">@color/text_light</item>

        <item name="android:colorBackground">@color/background_light</item>
        <item name="colorOnBackground">@color/text_dark</item>

        <item name="colorSurface">#fff</item>
        <item name="colorOnSurface">@color/text_dark</item>

        <item name="colorAccent">@color/spotify_green</item>

        <!-- Text colors -->
        <item name="android:textColor">@color/text_dark</item>
        <item name="android:textColorSecondary">@color/text_secondary_light</item>

        <!-- Divider -->
        <item name="android:divider">@color/divider_light</item>

        <item name="android:icon">@mipmap/ic_launcher</item>
        <item name="android:roundIcon" tools:ignore="NewApi">@mipmap/ic_launcher_round</item>

    </style>

    <style name="Theme.soundspottheme" parent="Base.Theme.soundspottheme" />

    <style name="ShapeAppearanceOverlay.App.CornerSize100Percent" parent="">
        <item name="cornerSize">50%</item>
    </style>

    <style name="TransparentText">
        <item name="android:textColor">#00000000</item>
    </style>

    <style name="CustomTheme" parent="Base.Theme.soundspottheme">
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:statusBarColor">#00000000</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>


    <!-- Base application theme. -->
    <style name="Theme.MusicApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
        <!-- Customize your theme here. -->
        <item name="android:windowTranslucentStatus">true</item>
    </style>

    <!-- Dark theme -->
    <style name="Theme.MusicApp.Dark" parent="Theme.MusicApp">
        <item name="android:windowBackground">@color/dark_background</item>
        <item name="colorSurface">@color/dark_surface</item>
        <item name="colorOnSurface">@color/white</item>
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColorSecondary">@color/dark_text_secondary</item>
    </style>

    <!-- Purple theme -->
    <style name="Theme.MusicApp.Purple" parent="Theme.MusicApp">
        <item name="android:windowBackground">@color/purple_background</item>
        <item name="colorSurface">@color/purple_surface</item>
        <item name="colorOnSurface">@color/purple_text_primary</item>
        <item name="android:textColorPrimary">@color/purple_text_primary</item>
        <item name="android:textColorSecondary">@color/purple_text_secondary</item>
    </style>


</resources>